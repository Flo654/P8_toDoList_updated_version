{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('task_list') }}" class="btn btn-primary mb-3">Retour à la liste des tâches</a>
    <div class="">
        {{ form_start(form, {'action' : path('task_create')}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success pull-right">Ajouter</button>
        {{ form_end(form) }}

        
    </div>
{% endblock %}
